#!/usr/bin/env bash
set -euo pipefail

echo "=== Daemon Resource Benchmark ==="
echo ""
echo "Status: SKIPPED (harness scaffold — no implementation yet)"
echo "Goal: RSS <=600MB including model memory"
echo "Goal: idle CPU ~0% when no keystroke requests are active"
